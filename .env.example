# Keycloak admin credentials (applied only on first boot)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change-me

# Optional: override host ports if defaults are occupied
# KEYCLOAK_HOST_PORT=8080
# BACKEND_HOST_PORT=8000

# Optional: enable OPC UA API + ingestion agent.
# Local docker-compose defaults OPCUA_ENABLED to false for safety.
# OPCUA_ENABLED=true

# Optional: allow additional Keycloak client IDs for API token validation
# KEYCLOAK_ALLOWED_CLIENT_IDS=dpp-backend,dpp-frontend

# Optional: Keycloak database overrides
# KEYCLOAK_DB_NAME=keycloak
# KEYCLOAK_DB_USERNAME=keycloak
# KEYCLOAK_DB_PASSWORD=keycloak_dev_password_2024

# Optional: GitHub token to avoid API rate limits when resolving IDTA templates
# IDTA_TEMPLATES_GITHUB_TOKEN=ghp_yourtokenhere
# Optional: UoM registry and enrichment controls (IDTA-01003-b)
# UOM_REGISTRY_ENABLED=true
# UOM_REGISTRY_SEED_PATH=/absolute/path/to/uom.seed.json
# UOM_ENRICHMENT_ENABLED=true
# UOM_VALIDATION_MODE=warn

# Optional: Alertmanager webhook targets (used by infra/monitoring/alertmanager/alertmanager.yml)
# ALERTMANAGER_WEBHOOK_URL=https://alerts.example.com/default
# ALERTMANAGER_WEBHOOK_URL_CRITICAL=https://alerts.example.com/critical

# Optional: default base URI for global asset IDs
# GLOBAL_ASSET_ID_BASE_URI_DEFAULT=http://localhost:8000/asset/

# Optional: CIRPASS lab public ingest/session settings
# CIRPASS_RESULTS_URL=https://cirpassproject.eu/project-results/
# CIRPASS_REFRESH_TTL_SECONDS=43200
# CIRPASS_SESSION_TTL_SECONDS=86400
# CIRPASS_SESSION_TOKEN_SECRET=change-me-cirpass-session-secret
# CIRPASS_LEADERBOARD_LIMIT_DEFAULT=20
# CIRPASS_LEADERBOARD_LIMIT_MAX=100
# CIRPASS_LAB_SCENARIO_ENGINE_ENABLED=true
# CIRPASS_LAB_LIVE_MODE_ENABLED=false
# CIRPASS_LAB_INSPECTOR_ENABLED=true
# CIRPASS_LAB_TELEMETRY_RETENTION_DAYS=30

# Optional: RFID TDS sidecar settings
# RFID_TDS_SERVICE_URL=http://rfid-tds:8080
# RFID_TDS_TIMEOUT_SECONDS=5
