[pytest]
testpaths = tests
addopts = -ra

markers =
    unit: fast tests, no external services
    integration: tests that use the in-process FastAPI app
    e2e: tests that require the full docker-compose stack
    golden: tests that compare against golden snapshots
    conformance: AAS conformance tests using aas-test-engines
