# Claim Register

This register maps README claims to code evidence and verification steps.
Status is based on static code review (no runtime validation performed).

| Claim | Where claimed (README) | Evidence expected | How to verify | Status / Notes |
| --- | --- | --- | --- | --- |
| DPP lifecycle management: create, edit, publish, archive | Features | API endpoints + service logic; UI flow | Use API: `POST /api/v1/dpps`, `PUT /api/v1/dpps/{id}/submodel`, `POST /api/v1/dpps/{id}/publish`, `POST /api/v1/dpps/{id}/archive` | **Partial**: Endpoints and service logic exist (`backend/app/modules/dpps/router.py`, `backend/app/modules/dpps/service.py`). UI pages exist (`frontend/src/features/publisher/pages/DPPListPage.tsx`, `frontend/src/features/publisher/pages/DPPEditorPage.tsx`). No end-to-end tests or runtime proof in repo. |
| IDTA DPP4.0 templates (6 templates) | Features, Initial Setup | Template registry service + pinned versions + UI listing | Call `POST /api/v1/templates/refresh` then `GET /api/v1/templates`; expect 6 | **Partial**: Template keys, semantic IDs, and pinned versions are defined (`backend/app/modules/templates/service.py`, `backend/app/core/config.py`). UI lists templates (`frontend/src/features/publisher/pages/TemplatesPage.tsx`). Requires external fetch and runtime confirmation. |
| OIDC authentication via Keycloak | Features, Quick Start | Keycloak realm config + backend JWT validation + frontend OIDC flow | Obtain token from Keycloak and call protected endpoints | **Pass (static)**: Keycloak realm export and docker service (`infra/keycloak/realm-export/dpp-platform-realm.json`, `docker-compose.yml`); backend JWT verification (`backend/app/core/security/oidc.py`); frontend OIDC client config (`frontend/src/main.tsx`). |
| ABAC authorization via OPA | Features | OPA policy engine integrated into request handling | Exercise allow/deny cases via OPA decisions | **Partial / Gap**: OPA service + Rego policies exist (`infra/opa/policies/dpp_authz.rego`, `docker-compose.yml`), and an ABAC client exists (`backend/app/core/security/abac.py`), but routes enforce RBAC checks directly and do not call OPA. No ABAC tests. |
| Catena-X DTR publishing (optional EDC DSP metadata) | Features, Connecting to Catena-X | DTR client, connector CRUD, publish flow | Configure connector, test, publish to DTR | **Pass (static)**: DTR connector + publish flow implemented (`backend/app/modules/connectors/catenax/*`, `frontend/src/features/connectors/pages/ConnectorsPage.tsx`). Optional `edc_dsp_endpoint` is metadata only. |
| Export AASX and JSON (AASX Part 5 compliant) | Features, Export Formats | Export endpoints + AASX package structure + validation | Call `GET /api/v1/export/{id}?format=aasx/json` and inspect AASX | **Partial**: Export endpoints exist (`backend/app/modules/export/router.py`) with AASX writer (`backend/app/modules/export/service.py`). No formal IDTA Part 5 validation, no conformance tests, and compliance is asserted only in docstrings. |
| QR code generation | Features | QR endpoint + QR service + UI action | Call `GET /api/v1/qr/{id}` | **Pass (static)**: QR service and endpoint exist (`backend/app/modules/qr/service.py`, `backend/app/modules/qr/router.py`), UI triggers QR generation (`frontend/src/features/publisher/pages/DPPEditorPage.tsx`). |
| Docker Compose local stack | Quick Start, Tech Stack | Compose file + service configs | `docker compose up -d` and check URLs | **Pass (static)**: Compose file defines Postgres, Redis, Keycloak, OPA, MinIO, backend, frontend (`docker-compose.yml`). |
| Tech stack claims (FastAPI, SQLAlchemy, Redis, Postgres, React, Vite, Tailwind) | Tech Stack | Dependencies and config files | Inspect `pyproject.toml`, `package.json` | **Pass (static)**: Backend and frontend dependencies align with README claims (`backend/pyproject.toml`, `frontend/package.json`). |
| Standards compliance IDs listed in README | Standards Compliance | Correct references to IDTA specs | Verify against IDTA catalog | **Fail / Needs correction**: README lists IDs like “IDTA 2002-1-0” for Part 1; code uses submodel semantic IDs, but README IDs appear inconsistent with IDTA catalog. Update README references for accuracy (`README.md`). |
