# =============================================================================
# Eclipse Dataspace Connector (EDC) — development compose overlay
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.edc.yml up -d
#
# This adds a Tractus-X EDC controlplane + dataplane pair with PostgreSQL
# persistence and a HashiCorp Vault instance for secret storage.
# =============================================================================

services:
  # ---- EDC Controlplane ----
  edc-controlplane:
    image: tractusx/edc-controlplane-postgresql-hashicorp-vault:0.7.3
    ports:
      - "19191:19191"   # Default / health
      - "19193:19193"   # Management API
      - "19194:19194"   # DSP protocol
    volumes:
      - ./infra/edc/config.properties:/app/configuration.properties:ro
      - ./infra/edc/logging.properties:/app/logging.properties:ro
    environment:
      EDC_FS_CONFIG: /app/configuration.properties
      JAVA_TOOL_OPTIONS: -Djava.util.logging.config.file=/app/logging.properties
      EDC_API_AUTH_KEY: ${EDC_API_KEY:-password}
      EDC_VAULT_HASHICORP_URL: http://vault:8200
      EDC_VAULT_HASHICORP_TOKEN: ${VAULT_DEV_ROOT_TOKEN:-dev-root-token}
    depends_on:
      edc-postgres:
        condition: service_healthy
      vault:
        condition: service_started
    networks:
      - default
      - dpp-network
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:19191/api/check/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  # ---- EDC Dataplane ----
  edc-dataplane:
    image: tractusx/edc-dataplane-hashicorp-vault:0.7.3
    ports:
      - "19291:19291"   # Public API (data transfers)
    environment:
      EDC_FS_CONFIG: /app/configuration.properties
      JAVA_TOOL_OPTIONS: -Djava.util.logging.config.file=/app/logging.properties
      EDC_VAULT_HASHICORP_URL: http://vault:8200
      EDC_VAULT_HASHICORP_TOKEN: ${VAULT_DEV_ROOT_TOKEN:-dev-root-token}
      # Dataplane-specific
      web.http.public.port: "19291"
      web.http.public.path: "/public"
      web.http.control.port: "19192"
      web.http.control.path: "/control"
    volumes:
      - ./infra/edc/config.properties:/app/configuration.properties:ro
      - ./infra/edc/logging.properties:/app/logging.properties:ro
    depends_on:
      edc-controlplane:
        condition: service_healthy
    networks:
      - default
      - dpp-network
    restart: unless-stopped

  # ---- EDC PostgreSQL ----
  edc-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: edc
      POSTGRES_USER: edc
      POSTGRES_PASSWORD: ${EDC_DB_PASSWORD:-edc_password}
    volumes:
      - edc-pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U edc -d edc"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  # ---- HashiCorp Vault (dev mode) ----
  vault:
    image: hashicorp/vault:1.15
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: ${VAULT_DEV_ROOT_TOKEN:-dev-root-token}
      VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
    ports:
      - "8200:8200"
    restart: unless-stopped

volumes:
  edc-pg-data:
